{
  "name": "uptaskapi",
  "version": "1.0.0",
  "description": "API para admin de tareas",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
